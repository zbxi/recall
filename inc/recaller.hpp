#pragma once

#include <algorithm>
#include <cassert>
#include <chrono>

namespace zbxi::recall
{
  class Recaller
  {
  public:
    Recaller() = default;
    ~Recaller() = delete;

  private:
  };
}
